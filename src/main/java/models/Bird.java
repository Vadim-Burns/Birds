package models;

import java.awt.*;

public abstract class Bird extends Actor {

    public Bird(double x, double y, String filePath) {
        super(x, y, filePath, new Point(0, 0));

        // Player bird sprite init

    }
}
